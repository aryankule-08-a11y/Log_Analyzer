# Parsed log data is stored here as Parquet files for fast reloading.
# 
# Generated by:
#   python cli.py --input data/raw/access.log --save-parquet data/processed/logs.parquet
#
# These files are gitignored (can be large). Regenerate from raw data.
